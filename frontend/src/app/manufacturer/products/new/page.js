import ProductForm from '@/components/manufacturer/ProductForm';

export const metadata = {
    title: 'Initialize Listing | GearUp Manufacturer',
};

export default function AddProductPage() {
    return <ProductForm />;
}
